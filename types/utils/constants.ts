export interface ConstantsType {
    colors: ColorTypes
}

interface ColorTypes {
    background: string,
    text: string,
    primary: string,
    secondary: string,
    lightText: string,
    price: string
}